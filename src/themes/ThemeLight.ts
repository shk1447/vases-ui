import { CustomOptions } from '.';
import { options } from './Common';

export default {
  pallete: {
    background: 'white',
    border: '#ccc',
  },
  background: 'white',
  color: 'black',
  border: 'black',
  ...options,
} as CustomOptions;
