import { CustomOptions } from '.';
import { options } from './Common';
export default {
  pallete: {
    background: '#202020',
    border: 'rgba(0,0,0,0)',
  },
  background: 'black',
  color: 'white',
  border: 'white',
  ...options,
} as CustomOptions;
